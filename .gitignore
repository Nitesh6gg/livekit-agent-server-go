# Build output
/bin/
/dist/
*.exe
*.test
*.out

# Environment / secrets
.env
.env.*
!.env.example

# Go
/vendor/
go.work
go.work.sum

# Logs & profiling
*.log
*.pprof
/profiles/

# Benchmark output: raw samples, profiles and generated compose overrides.
# summary.md and ablation.csv are worth keeping when a run informs a decision —
# copy those into docs/ or attach them to the ADR rather than committing the tree.
/bench/results/

# Editor
.idea/
.vscode/
.DS_Store
